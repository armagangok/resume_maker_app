import 'package:flutter/material.dart';

const radius25 = Radius.circular(25.0);
const radius16 = Radius.circular(16.0);
const radius13 = Radius.circular(13.0);

BoxDecoration get decorationRadius13 => const BoxDecoration(
      borderRadius: BorderRadius.all(radius13),
      color: Colors.white,
    );
